---
"@mkowalew-dev/careconnect-design-system": patch
---

Fix missing design tokens/reset in published CSS; externalize runtime dependencies from the build to reduce bundle size and avoid duplicate copies in consumers.
